"""Geplante Jobs (launchd/cron): Weekly-Report, Gym-Reminder.

Siehe `trainer.jobs.weekly_report` und `trainer.jobs.reminder_check` für die
CLI-Einstiegspunkte sowie `trainer.jobs.notify` für den gemeinsamen
Telegram-Versand.
"""
